generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["public"]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model options {
  id              String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  question_id     String            @db.Uuid
  label           String            @db.Char(1)
  text            String
  is_correct      Boolean           @default(false)
  questions       questions         @relation(fields: [question_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_answers student_answers[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model questions {
  id              String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  task_id         String            @db.Uuid
  order_index     Int
  prompt          Json
  created_at      DateTime?         @default(now()) @db.Timestamptz(6)
  options         options[]
  tasks           tasks             @relation(fields: [task_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_answers student_answers[]

  @@unique([task_id, order_index], map: "unique_question_order")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_answers {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id     String    @db.Uuid   // Supabase auth user id (scalar only)
  question_id String    @db.Uuid
  option_id   String    @db.Uuid
  is_correct  Boolean
  answered_at DateTime? @default(now()) @db.Timestamptz(6)

  options   options   @relation(fields: [option_id], references: [id], onDelete: NoAction)
  questions questions @relation(fields: [question_id], references: [id], onDelete: Cascade)

  @@unique([user_id, question_id], map: "unique_student_question")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tasks {
  id           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title        String
  instructions String
  created_at   DateTime?   @default(now()) @db.Timestamptz(6)
  questions    questions[]

  @@schema("public")
}
